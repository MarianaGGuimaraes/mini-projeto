let idade: number;

idade = 25;

//o mesmo serve para função. 

function somaIdade(idadeInicial: number, anosSePassaram:number){
    return idadeInicial + anosSePassaram;
}

somaIdade(25, 1);